const puppeteer = require("puppeteer");
const fs = require("fs");
const readline = require("readline");
const nmap = require("node-nmap");

module.exports = {
  puppeteer,
  fs,
  readline,
  nmap,
};
